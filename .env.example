# Public (se expone al navegador si se usa en componentes cliente)
# Úsalas para personalizar el sitio sin recompilar.
NEXT_PUBLIC_SITE_NAME="BlogSalud"
NEXT_PUBLIC_SITE_DESCRIPTION="Artículos de salud femenina."
NEXT_PUBLIC_SITE_URL="https://tudominio.com"

# Server-only (ejemplos; NO poner secretos aquí)
# RESEND_API_KEY=""
# DATABASE_URL=""

# Admin / Autoría (Server-only)
# Se usan para autenticar el panel /admin.
# - ADMIN_USERNAME: usuario para acceder a /admin
# - ADMIN_PASSWORD: contraseña del usuario anterior
# - ADMIN_COOKIE_SECRET: secreto del servidor para firmar/verificar la cookie de sesión.
#   Debe ser largo/aleatorio. Si cambia, se invalidan sesiones activas.
ADMIN_USERNAME="admin"
ADMIN_PASSWORD=""
ADMIN_COOKIE_SECRET=""

# Publicación automática a GitHub (Server-only)
# El servidor usa GitHub API para crear commits al publicar/editar/eliminar artículos
# y para subir/borrar imágenes en public/images/.
# - GITHUB_TOKEN: token con permisos de escritura sobre el repo (Contents/Git data).
# - GITHUB_OWNER: usuario u organización dueña del repo.
# - GITHUB_REPO: nombre del repo.
GITHUB_TOKEN=""
GITHUB_OWNER=""
GITHUB_REPO=""

# Separación DEV/PRO del contenido (Server-only)
# El branch se selecciona automáticamente según el entorno:
# - Si defines CONTENT_ENV, fuerza el entorno de contenido: dev|prod
# - Si no, se infiere por VERCEL_ENV (si estás en Vercel) o NODE_ENV.
CONTENT_ENV="dev"

# Branches recomendados por entorno:
# - GITHUB_BRANCH_PROD: branch que representa PRODUCCIÓN (p.ej. main)
# - GITHUB_BRANCH_DEV: branch que representa DESARROLLO/preview (p.ej. dev o content-dev)
GITHUB_BRANCH_PROD="main"
GITHUB_BRANCH_DEV="dev"

# Legacy (compatibilidad): un solo branch para ambos entornos.
# Si defines solo esta variable, se usa tanto en dev como en prod.
GITHUB_BRANCH="main"
